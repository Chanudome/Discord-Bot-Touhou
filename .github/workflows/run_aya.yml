# ไฟล์นี้ต้องอยู่ที่ .github/workflows/run_aya.yml ใน Repository ของคุณ

name: Aya News Bot (Auto Run)

on:
  schedule:
    # ตั้งเวลาให้รันทุกๆ 30 นาที (ปรับเปลี่ยนได้ตามต้องการ)
    - cron: '*/30 * * * *'
  workflow_dispatch: # อนุญาตให้กดปุ่มรันเองได้

jobs:
  check-news:
    runs-on: ubuntu-latest
    
    # อนุญาตให้บอทเขียนไฟล์ (เพื่อบันทึกประวัติการอ่านลง repo)
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Aya Bot
        env:
          # ดึง Key จาก GitHub Secrets
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python main.py

      - name: Commit and Push History
        # บันทึกไฟล์ aya_news_history.json กลับเข้า Repo
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "📝 Aya updated news history [skip ci]"
          file_pattern: "*.json"
