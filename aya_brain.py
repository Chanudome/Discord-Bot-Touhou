from google import genai
import os
import time

# ดึง Key จาก Environment Variable
api_key = os.getenv("GEMINI_API_KEY")

# สร้าง Client สำหรับเชื่อมต่อ
client = None
if api_key:
    try:
        client = genai.Client(api_key=api_key)
    except Exception as e:
        print(f"⚠️ Error ในการตั้งค่า AI Client: {e}")

def get_valid_models():
    """
    ดึงรายชื่อโมเดลทั้งหมดจาก Key นี้โดยตรง (แบบปลอดภัย)
    """
    if not client: return []
    
    discovered_models = []
    try:
        print("🔍 กำลังสแกนหาโมเดลสำรองจาก API...")
        for m in client.models.list():
            model_name = getattr(m, 'name', '')
            if 'gemini' in model_name:
                clean_name = model_name.replace('models/', '')
                if 'tts' in clean_name or 'audio' in clean_name or 'embedding' in clean_name:
                    continue
                if 'flash' in clean_name or 'pro' in clean_name:
                    discovered_models.append(clean_name)
        
        print(f"   -> เจอโมเดลสำรองที่ใช้ได้จริง: {len(discovered_models)} ตัว")
    except Exception as e:
        print(f"⚠️ สแกนโมเดลไม่สำเร็จ: {e}")
    
    return discovered_models

def aya_process_news(source_type, title, content, link, pub_date):
    if not client:
        return "AI_ERROR: Client not initialized (Check API Key)"

    system_instruction = """
    Roleplay: คุณคือ "Shameimaru Aya" (ชามะเมารุ อายะ) นักข่าวเท็นงูแห่งหนังสือพิมพ์บุนบุนมารุ
    Target Audience: แฟนเกม Touhou Project ชาวไทย
    Core Directive:
    1. อ่านข่าวและแปลเป็นภาษาไทยที่เข้าใจง่าย
    2. แยกแยะ "ข้อเท็จจริง" (Report) ออกจาก "เนื้อเรื่อง/ตำนาน" (Lore)
    3. ใช้น้ำเสียงร่าเริง กระตือรือร้น (ขึ้นต้นด้วย "อายะยะ!", "ข่าวด่วน!")
    """

    specific_task = ""
    if source_type == "game_update":
        specific_task = "ข่าวเกม: แยกเนื้อหาอัปเดตกับเนื้อเรื่องย่อ(Prologue)"
    elif source_type == "official":
        specific_task = "ข่าวทางการ: สรุปวันเวลาสถานที่ให้ครบถ้วน"
    elif source_type == "magazine":
        specific_task = "บทความ: สรุปประเด็นสำคัญ"
    elif source_type == "community":
        specific_task = "Reddit: ถ้าเป็น Fan Art ให้บรรยายภาพสั้นๆ ห้ามแต่ง Lore เพิ่ม! ถ้าเป็นข่าวลือให้สรุปมา"

    full_prompt = f"""
    {system_instruction}
    --------------------------------
    คำสั่งเพิ่มเติม: {specific_task}
    
    [ข้อมูลข่าวต้นทาง]
    Headline: {title}
    Date: {pub_date}
    Content Snippet: {content}
    Link: {link}
    
    [รูปแบบการตอบกลับ]
    (หากเนื้อหาไร้สาระให้พิมพ์: SKIP)
    
    📷 **[พาดหัวข่าวสไตล์บุนบุนมารุ]**
    "อายะยะ! <บทนำสั้นๆ สไตล์อายะ>"
    
    📜 **เนื้อหาข่าว:**
    <สรุปรายละเอียดข่าว ข้อเท็จจริง เป็นข้อๆ>
    - ...
    
    (ใส่เฉพาะเมื่อมี Lore จริงๆ ห้ามใส่ใน Fan Art)
    📖 **เกร็ดตำนาน/เนื้อเรื่องย่อ:**
    <แปลสรุปเนื้อเรื่องย่อ>
    
    🌪️ **ความเห็นของอายะ:** <วิเคราะห์ข่าว หรือแซวตัวละคร>
    """

    # [รายการที่คัดกรองแล้ว] เหลือเฉพาะตัวที่มีอยู่จริง (ตาม Log ของคุณ)
    models_to_try = [
        "gemini-2.0-flash-exp",      # ตัวหลัก (ติด 429 บ่อย แต่มีของ)
        "gemini-2.0-flash",          # ตัวรอง
        "gemini-2.5-flash",          # ตัวใหม่
        "gemini-2.5-pro",            # ตัวโปร
    ]

    # ลองเพิ่มโมเดลที่สแกนเจอเอง (ถ้ามีเพิ่มเติมนอกเหนือจากรายการข้างบน)
    try:
        fallback_list = get_valid_models()
        for m in fallback_list:
            if m not in models_to_try:
                models_to_try.append(m)
    except Exception as e:
        print(f"⚠️ ข้ามการสแกนโมเดลอัตโนมัติ: {e}")

    # ระบบ Retry
    for i, model_name in enumerate(models_to_try):
        try:
            response = client.models.generate_content(
                model=model_name,
                contents=full_prompt
            )
            return response.text.strip()
            
        except Exception as e:
            error_msg = str(e)
            
            # 429 Quota Exceeded (พักยาว เพื่อรอคิวว่าง)
            if "429" in error_msg:
                wait_time = 60 * (i + 1)
                if wait_time > 300: wait_time = 300 
                print(f"⚠️ โมเดล {model_name} โควต้าเต็ม (429).. พัก {wait_time} วิ แล้วลองตัวถัดไป")
                time.sleep(wait_time)
            
            # 503 Server Error (พักสั้นแล้วลองใหม่หรือข้าม)
            elif "503" in error_msg:
                print(f"⚠️ โมเดล {model_name} เซิร์ฟเวอร์ล่มชั่วคราว (503).. ข้ามไปตัวอื่น")
            
            # 404 หรืออื่นๆ (ข้ามเลย)
            else:
                print(f"⚠️ โมเดล {model_name} ใช้ไม่ได้ ({error_msg}) -> ข้าม")
    
    return "AI_ERROR: ไม่สามารถแปลข่าวได้ (ลองทุกโมเดลแล้ว)"
