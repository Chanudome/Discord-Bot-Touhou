import google.generativeai as genai
import os

# ดึง Key จาก Environment Variable
api_key = os.getenv("GEMINI_API_KEY")

# ตัวแปรสำหรับเก็บโมเดล
model = None

if api_key:
    genai.configure(api_key=api_key)
    
    # --- [ระบบใหม่] ค้นหาโมเดลอัตโนมัติ (Auto-detect) ---
    try:
        print("🔍 กำลังค้นหาโมเดล AI ที่ใช้งานได้...")
        # ดึงรายชื่อโมเดลทั้งหมดที่ Key นี้ใช้ได้
        all_models = list(genai.list_models())
        
        # คัดเฉพาะรุ่นที่สร้างข้อความได้ (generateContent)
        valid_models = [
            m.name for m in all_models 
            if 'generateContent' in m.supported_generation_methods
        ]
        
        # ลำดับรุ่นที่เราอยากได้ (ใหม่ -> เก่า)
        preferred_order = [
            'gemini-1.5-flash',
            'gemini-1.5-pro',
            'gemini-1.0-pro', 
            'gemini-pro'
        ]
        
        selected_model_name = None
        
        # วนลูปหาว่ามีรุ่นที่เราอยากได้ไหม
        for pref in preferred_order:
            for m_name in valid_models:
                if pref in m_name:
                    selected_model_name = m_name
                    break
            if selected_model_name:
                break
        
        # ถ้าหาไม่เจอเลย ให้เอาอันแรกสุดที่มี
        if not selected_model_name and valid_models:
            selected_model_name = valid_models[0]
            
        if selected_model_name:
            print(f"🤖 พบโมเดลที่ใช้ได้: {selected_model_name}")
            model = genai.GenerativeModel(selected_model_name)
        else:
            print("❌ Error: ไม่พบโมเดล AI ที่รองรับ generateContent สำหรับ Key นี้")
            
    except Exception as e:
        print(f"⚠️ Error ในการค้นหาโมเดล: {e}")
        # ถ้าค้นหาไม่เจอจริงๆ ให้ลองเสี่ยงดวงใช้ตัวมาตรฐาน
        model = genai.GenerativeModel('gemini-1.5-flash')
else:
    print("⚠️ Warning: ไม่พบ GEMINI_API_KEY ใน Environment Variables")

def aya_process_news(source_type, title, content, link, pub_date):
    """
    ฟังก์ชันหลักในการส่งข้อมูลให้ AI สวมบทบาทและแปลข่าว
    """
    if not model:
        return "AI_ERROR: Model not initialized (Check API Key)"

    try:
        # --- System Prompt: กำหนดบุคลิกและหน้าที่ ---
        system_instruction = """
        Roleplay: คุณคือ "Shameimaru Aya" (ชามะเมารุ อายะ) นักข่าวเท็นงูแห่งหนังสือพิมพ์บุนบุนมารุ
        Target Audience: แฟนเกม Touhou Project ชาวไทย
        
        Core Directive:
        1. อ่านข่าวและแปลเป็นภาษาไทยที่เข้าใจง่าย
        2. แยกแยะ "ข้อเท็จจริง" (Report) ออกจาก "เนื้อเรื่อง/ตำนาน" (Lore)
        3. ใช้น้ำเสียงร่าเริง กระตือรือร้น (ขึ้นต้นด้วย "อายะยะ!", "ข่าวด่วน!")
        4. เรียก ZUN ว่า "ท่านคันนุชิ"
        """

        # --- Context Instruction ---
        specific_task = ""
        if source_type == "game_update":
            specific_task = "ข่าวเกม/Patch Note: ให้แยก 'เนื้อหาอัปเดต' กับ 'เนื้อเรื่องย่อ(Prologue)' ออกจากกัน ถ้ามีเนื้อเรื่องต้องแปลมาด้วย!"
        elif source_type == "official":
            specific_task = "ข่าวทางการ: สรุป วัน/เวลา/สถานที่/ราคา ให้ครบถ้วนชัดเจน"
        elif source_type == "magazine":
            specific_task = "บทความ/สัมภาษณ์: สรุปประเด็นสำคัญ 3-4 ข้อ"
        elif source_type == "community":
            specific_task = "Reddit: ถ้าเป็นแค่รูปภาพ/มีมตลกๆ ให้พิมพ์คำเดียวว่า 'SKIP' แต่ถ้าเป็นข่าวลือหรือประเด็นน่าสนใจ ให้เล่ามา"

        full_prompt = f"""
        {system_instruction}
        --------------------------------
        คำสั่งเพิ่มเติม: {specific_task}
        
        [ข้อมูลข่าวต้นทาง]
        Headline: {title}
        Date: {pub_date}
        Content Snippet: {content}
        Link: {link}
        
        [รูปแบบการตอบกลับ]
        (หากเนื้อหาไร้สาระให้พิมพ์: SKIP)
        
        📷 **[พาดหัวข่าวสไตล์บุนบุนมารุ]**
        "อายะยะ! <บทนำสั้นๆ สไตล์อายะ>"
        
        📜 **เนื้อหาข่าว:**
        <สรุปรายละเอียดข่าว ข้อเท็จจริง เป็นข้อๆ>
        - ...
        
        (ใส่เฉพาะเมื่อมี Lore)
        📖 **เกร็ดตำนาน/เนื้อเรื่องย่อ:**
        <แปลสรุปเนื้อเรื่องย่อ>
        
        🌪️ **ความเห็นของอายะ:** <วิเคราะห์ข่าว หรือแซวตัวละคร>
        """

        response = model.generate_content(full_prompt)
        return response.text.strip()

    except Exception as e:
        return f"AI_ERROR: {str(e)}"
